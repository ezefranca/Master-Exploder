#include "controle.h"
#include <math.h>
#include "fecho.h"
#include "comum.h"

int calcula_padrao(poligono *fecho, unsigned char ***imagem_da_mao){

	//ponto centroide;
    centroide(fecho, centroide);
    //conta_pb(centroide[X], centroide[Y], imagem_da_mao);
	printf("PB %d %d\n", qtd_branco, qtd_preto);
  
	return 0;
}